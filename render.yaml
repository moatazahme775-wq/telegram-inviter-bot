services:
  - type: web
    name: telegram-mass-inviter
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 18.16.0
    disk:
      name: bot-data
      mountPath: /app/data
      sizeGB: 1
